function [integral] = latinHypercube_integrate(f, randPoints)
% Based on page 360 of Handbook of MC methods by
% Dirk P. Kroese, Thomas Taimre, and Zdravko I. Botev

K = 7;

us = rand(2,K);
xs = -us / K;
for k=1:K
    xs = xs + (randperm(K) + 1)/k
end

integral = 0;

end